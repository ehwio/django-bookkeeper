[pytest]
DJANGO_SETTINGS_MODULE = tests.e2e_settings
pythonpath = src .
testpaths = tests/e2e
addopts = --browser chromium --headed=false
markers =
    e2e: Playwright end-to-end tests (require live server and browser)
